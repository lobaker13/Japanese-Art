document.addEventListener("ajax:complete",function(event){
    console.log(event);
    if( event.target.classList.contains("delete-lease")) {
      event.target.parentNode.parentNode.remove();
    }
  });
