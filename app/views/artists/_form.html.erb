<%= form_with(model: artist, local: true) do |form| %>
  <% if artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :artist_name %>
  </div>

  <div class="field">
    <%= form.label :bio %>
    <%= form.text_area :bio, id: :artist_bio %>
  </div>

  <div class="field">
    <%= form.label :dob %>
    <%= form.number_field :dob, id: :artist_dob %>
  </div>

  <div class="field">
    <%= form.label :dod %>
    <%= form.number_field :dod, id: :artist_dod %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
